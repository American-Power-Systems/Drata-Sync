Start with **one file: `main.py`** (Flask app). This will:

* Pull “security training completions” from a simple source (for now: a JSON POST you can send, or a CSV you drop into Replit)
* Push the latest records into **Drata → Custom Connection** using your **Drata API key**
* Provide **email + completion date + proof** fields Drata can use as evidence

Below is a complete `main.py` you can copy/paste into Replit.

> If you already have a Replit project started, paste this into your existing **`main.py`** (or whatever your main file is), and tell me the filename if it’s different.

```python
import os
import json
import csv
import time
from datetime import datetime, timezone
from typing import List, Dict, Any, Optional

import requests
from flask import Flask, request, jsonify

app = Flask(__name__)

# =========================
# Config (Replit Secrets)
# =========================
# Required
DRATA_API_KEY = os.getenv("DRATA_API_KEY", "").strip()

# You may need these depending on how Drata Custom Connection is set up
# (You can leave them blank until you know the exact endpoint/IDs.)
DRATA_BASE_URL = os.getenv("DRATA_BASE_URL", "https://api.drata.com").strip()  # placeholder
DRATA_WORKSPACE_ID = os.getenv("DRATA_WORKSPACE_ID", "").strip()
DRATA_DATASOURCE_ID = os.getenv("DRATA_DATASOURCE_ID", "").strip()

# Optional: file-based import
CSV_PATH = os.getenv("TRAINING_CSV_PATH", "training_completions.csv").strip()

# Optional: simple shared secret to protect your endpoints
APP_AUTH_TOKEN = os.getenv("APP_AUTH_TOKEN", "").strip()


# =========================
# Helpers
# =========================
def require_auth():
    """Optional bearer-token protection for your endpoints."""
    if not APP_AUTH_TOKEN:
        return  # not enforced
    auth = request.headers.get("Authorization", "")
    if auth != f"Bearer {APP_AUTH_TOKEN}":
        return jsonify({"error": "Unauthorized"}), 401


def utc_now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def normalize_record(rec: Dict[str, Any]) -> Dict[str, Any]:
    """
    Normalize a training completion record into a consistent schema we can push to Drata.
    Required for your requirement: email + proof + completion time.
    """
    email = (rec.get("employee_email") or rec.get("email") or "").strip().lower()
    if not email:
        raise ValueError("Missing employee email")

    training_name = (rec.get("training_name") or rec.get("course") or "Security Training").strip()

    completed_at = rec.get("completed_at") or rec.get("completion_date") or rec.get("completedAt")
    if not completed_at:
        raise ValueError(f"Missing completed_at for {email}")

    # Accept ISO, or common date strings; store ISO UTC if possible
    completed_iso = parse_date_to_iso(completed_at)

    # Proof can be a URL to certificate, screenshot, LMS completion page, etc.
    proof_url = (rec.get("proof_url") or rec.get("certificate_url") or rec.get("proofUrl") or "").strip()
    proof_text = (rec.get("proof_text") or rec.get("proofText") or "").strip()

    # Some orgs store “score” or “status”
    status = (rec.get("status") or "completed").strip().lower()

    return {
        "employee_email": email,
        "training_name": training_name,
        "completed_at": completed_iso,
        "status": status,
        "proof_url": proof_url,
        "proof_text": proof_text,
        "source": rec.get("source", "replit-sync"),
        "synced_at": utc_now_iso(),
    }


def parse_date_to_iso(value: str) -> str:
    """
    Tries to parse a date/time string and return ISO8601.
    If it's already ISO, keep it.
    """
    s = str(value).strip()
    # If it looks ISO-ish, try fromisoformat
    try:
        # fromisoformat doesn't accept Z, so normalize
        s2 = s.replace("Z", "+00:00")
        dt = datetime.fromisoformat(s2)
        # Ensure timezone; if none, assume UTC
        if dt.tzinfo is None:
            dt = dt.replace(tzinfo=timezone.utc)
        return dt.astimezone(timezone.utc).isoformat()
    except Exception:
        pass

    # Try a few common formats
    fmts = [
        "%m/%d/%Y",
        "%m/%d/%Y %H:%M",
        "%m/%d/%Y %H:%M:%S",
        "%Y-%m-%d",
        "%Y-%m-%d %H:%M",
        "%Y-%m-%d %H:%M:%S",
    ]
    for fmt in fmts:
        try:
            dt = datetime.strptime(s, fmt).replace(tzinfo=timezone.utc)
            return dt.isoformat()
        except Exception:
            continue

    raise ValueError(f"Unrecognized date format: {value}")


def load_records_from_csv(path: str) -> List[Dict[str, Any]]:
    """
    CSV columns supported (minimum):
      employee_email, completed_at
    Recommended:
      training_name, proof_url, status
    """
    if not os.path.exists(path):
        return []

    out: List[Dict[str, Any]] = []
    with open(path, "r", newline="", encoding="utf-8-sig") as f:
        reader = csv.DictReader(f)
        for row in reader:
            out.append(row)
    return out


# =========================
# Drata Push (placeholder)
# =========================
def drata_headers() -> Dict[str, str]:
    if not DRATA_API_KEY:
        raise RuntimeError("Missing DRATA_API_KEY (set it in Replit Secrets).")
    return {
        "Authorization": f"Bearer {DRATA_API_KEY}",
        "Content-Type": "application/json",
        "Accept": "application/json",
    }


def push_to_drata_custom_connection(records: List[Dict[str, Any]]) -> Dict[str, Any]:
    """
    IMPORTANT:
    Drata’s Custom Connection ingestion endpoint can vary depending on UI setup.
    This function is written to be easy to adjust once you confirm the endpoint.

    For now it expects:
      DRATA_BASE_URL, DRATA_WORKSPACE_ID, DRATA_DATASOURCE_ID

    If you paste your Drata custom connection API endpoint format, I’ll adjust this
    precisely in the next step.
    """
    if not DRATA_WORKSPACE_ID or not DRATA_DATASOURCE_ID:
        return {
            "ok": False,
            "error": "Missing DRATA_WORKSPACE_ID or DRATA_DATASOURCE_ID. Set these in Replit Secrets once you know them.",
            "sent": 0,
        }

    # Placeholder URL pattern — YOU WILL LIKELY NEED TO UPDATE THIS.
    # Example pattern could be something like:
    #   /v1/workspaces/{workspaceId}/custom-connections/{dataSourceId}/records
    url = f"{DRATA_BASE_URL}/v1/workspaces/{DRATA_WORKSPACE_ID}/custom-connections/{DRATA_DATASOURCE_ID}/records"

    payload = {"records": records}

    r = requests.post(url, headers=drata_headers(), data=json.dumps(payload), timeout=60)
    if r.status_code >= 300:
        return {
            "ok": False,
            "status_code": r.status_code,
            "response_text": r.text[:2000],
            "sent": len(records),
            "url_used": url,
        }

    try:
        body = r.json()
    except Exception:
        body = {"raw": r.text}

    return {"ok": True, "sent": len(records), "drata_response": body, "url_used": url}


# =========================
# Routes
# =========================
@app.get("/health")
def health():
    return jsonify({"ok": True, "time": utc_now_iso()})


@app.post("/training/import")
def training_import():
    maybe = require_auth()
    if maybe:
        return maybe

    """
    POST JSON like:
    {
      "records": [
        {
          "employee_email": "user@company.com",
          "training_name": "KnowBe4 Security Awareness",
          "completed_at": "2026-02-24T18:20:00Z",
          "proof_url": "https://...",
          "status": "completed"
        }
      ]
    }
    """
    data = request.get_json(silent=True) or {}
    raw_records = data.get("records") or []
    if not isinstance(raw_records, list):
        return jsonify({"error": "records must be a list"}), 400

    normalized = []
    errors = []
    for i, rec in enumerate(raw_records):
        try:
            normalized.append(normalize_record(rec))
        except Exception as e:
            errors.append({"index": i, "error": str(e), "record": rec})

    return jsonify({"imported": len(normalized), "errors": errors, "normalized_preview": normalized[:5]})


@app.post("/sync")
def sync():
    maybe = require_auth()
    if maybe:
        return maybe

    """
    Runs a sync:
    - Loads CSV (optional)
    - AND/OR accepts inline records
    - Normalizes
    - Pushes to Drata
    """
    data = request.get_json(silent=True) or {}
    inline_records = data.get("records") if isinstance(data.get("records"), list) else []
    use_csv = bool(data.get("use_csv", True))

    raw_records: List[Dict[str, Any]] = []
    if use_csv:
        raw_records.extend(load_records_from_csv(CSV_PATH))
    raw_records.extend(inline_records)

    if not raw_records:
        return jsonify({"ok": False, "error": "No records found. Provide records or upload training_completions.csv."}), 400

    normalized: List[Dict[str, Any]] = []
    errors: List[Dict[str, Any]] = []
    for i, rec in enumerate(raw_records):
        try:
            normalized.append(normalize_record(rec))
        except Exception as e:
            errors.append({"index": i, "error": str(e), "record": rec})

    # Push to Drata
    try:
        result = push_to_drata_custom_connection(normalized)
    except Exception as e:
        return jsonify({"ok": False, "error": str(e), "normalized": len(normalized), "errors": errors}), 500

    return jsonify({
        "ok": result.get("ok", False),
        "normalized": len(normalized),
        "errors": errors,
        "drata": result,
    })


# For Replit
if __name__ == "__main__":
    port = int(os.getenv("PORT", "3000"))
    app.run(host="0.0.0.0", port=port, debug=True)
```

## JSON schema you can paste into Drata (Custom Connection)

Paste this into Drata’s **JSON schema** box:

```json
{
  "type": "object",
  "properties": {
    "records": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["employee_email", "training_name", "completed_at"],
        "properties": {
          "employee_email": { "type": "string" },
          "training_name": { "type": "string" },
          "completed_at": { "type": "string" },
          "status": { "type": "string" },
          "proof_url": { "type": "string" },
          "proof_text": { "type": "string" },
          "source": { "type": "string" },
          "synced_at": { "type": "string" }
        }
      }
    }
  },
  "required": ["records"]
}
```

## Sample JSON data

```json
{
  "records": [
    {
      "employee_email": "stephshockley@ampowersys.com",
      "training_name": "Security Awareness Training",
      "completed_at": "2026-02-24T18:20:00Z",
      "status": "completed",
      "proof_url": "https://example.com/certificates/stephshockley.pdf"
    }
  ]
}
```

---

## One thing I need from you (to wire Drata correctly)

Drata Custom Connections require a specific **ingestion endpoint** + identifiers.

**Send me just this (single item):**

* a screenshot or copy/paste of the Drata Custom Connection page that shows the **API URL / endpoint** (or anything like “Send data to … /v1/…”) for the data source.

That’s the one missing piece to replace the placeholder URL in `push_to_drata_custom_connection()`.

---

## Next step (do this now)

1. Paste the code into `main.py` in Replit
2. Add **Replit Secrets**:

* `DRATA_API_KEY` = your API key

Then tell me the **Drata Custom Connection ingestion URL** (or screenshot), and I’ll update the code to post to the exact correct endpoint.
